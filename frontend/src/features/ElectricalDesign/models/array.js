/**
 * array.js — P2+ entity factory/validator for electrical arrays (panels grouped by orientation).
 *
 * Do NOT import or call from Task A scaffolding.
 */

/** @throws always — not implemented until P2 */
export function createArray() {
  throw new Error("Electrical array model wired in P2 — not available in Task A");
}
