/**
 * inverter.js — P4+ entity factory/validator for selected inverters and catalog entries.
 *
 * Do NOT import or call from Task A scaffolding.
 */

/** @throws always — not implemented until P4 */
export function createInverter() {
  throw new Error("Electrical inverter model wired in P4 — not available in Task A");
}
