/**
 * string.js — P3+ entity factory/validator for panel strings within an array.
 *
 * Do NOT import or call from Task A scaffolding.
 */

/** @throws always — not implemented until P3 */
export function createString() {
  throw new Error("Electrical string model wired in P3 — not available in Task A");
}
