/**
 * mppt.js — P4+ entity factory/validator for MPPT inputs and string assignments.
 *
 * Do NOT import or call from Task A scaffolding.
 */

/** @throws always — not implemented until P4 */
export function createMppt() {
  throw new Error("Electrical MPPT model wired in P4 — not available in Task A");
}
