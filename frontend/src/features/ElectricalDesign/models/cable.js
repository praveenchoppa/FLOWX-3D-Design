/**
 * cable.js — P6+ entity factory/validator for logical and routed cable segments.
 *
 * Do NOT import or call from Task A scaffolding.
 */

/** @throws always — not implemented until P6 */
export function createCable() {
  throw new Error("Electrical cable model wired in P6 — not available in Task A");
}
