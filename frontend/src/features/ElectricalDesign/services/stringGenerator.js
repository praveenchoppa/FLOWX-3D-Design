/**
 * stringGenerator.js — P3+ automatic and manual string creation from array panel sets.
 *
 * Do NOT import or call from Task A scaffolding.
 */

/** @throws always — not implemented until P3 */
export function generateStrings() {
  throw new Error("stringGenerator wired in P3 — not available in Task A");
}
