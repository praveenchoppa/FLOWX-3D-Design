/**
 * useElectricalStore.js — P1 Task B hook to read/write electricalStore context.
 *
 * Do NOT import or call from Task A scaffolding.
 */

/** @throws always — not implemented until P1 Task B */
export function useElectricalStore() {
  throw new Error("useElectricalStore wired in P1 Task B — not available in Task A");
}
