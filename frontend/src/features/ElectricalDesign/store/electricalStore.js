/**
 * electricalStore.js — P1 Task B React context/reducer for electrical design state.
 *
 * Will hold arrays, strings, inverters, MPPT assignments, and cable routing data.
 * Do NOT import or wire from Task A scaffolding.
 */

/** Marker export so the module is a valid ES module until Task B implements the store. */
export const ELECTRICAL_STORE_PLACEHOLDER = true;
