/**
 * geometry.js — P6+ geometric helpers for panel positions, cable paths, and routing constraints.
 *
 * Do NOT import or call from Task A scaffolding.
 */

/** @throws always — not implemented until P6 */
export function geometryHelpers() {
  throw new Error("geometry utils wired in P6 — not available in Task A");
}
