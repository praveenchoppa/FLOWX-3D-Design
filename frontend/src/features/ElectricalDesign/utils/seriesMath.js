/**
 * seriesMath.js — P5+ series/parallel electrical math helpers (V adds, I constant, etc.).
 *
 * Do NOT import or call from Task A scaffolding.
 */

/** @throws always — not implemented until P5 */
export function seriesMath() {
  throw new Error("seriesMath utils wired in P5 — not available in Task A");
}
