/**
 * ArrayTree.jsx — P1+ left sidebar tree (arrays → strings) and selection affordances.
 *
 * Not imported anywhere in Task A scaffolding.
 */
export default function ArrayTree() {
  return null;
}
