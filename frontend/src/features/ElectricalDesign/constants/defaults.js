/**
 * defaults.js — P1+ default electrical design constants (modes, cable types, tolerances).
 *
 * Do NOT import from Task A scaffolding.
 */

/** Empty placeholder until Task B defines module defaults. */
export const ELECTRICAL_DEFAULTS = {};
