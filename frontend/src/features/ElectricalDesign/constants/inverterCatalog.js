/**
 * inverterCatalog.js — P4+ predefined inverter catalog entries for selection UI.
 *
 * Do NOT import from Task A scaffolding.
 */

/** Empty placeholder until P4 populates the catalog. */
export const INVERTER_CATALOG = [];
